import { Meta, Story } from "@storybook/addon-docs";
import Textarea from "./Textarea";

<Meta title="Components/Textarea" component={Textarea} />

# Textarea Component Documentation

## Overview

The `Textarea` component is designed for multi-line text input, providing a customizable, flexible field that supports different sizes, variants, and resizing options. It is ideal for situations where more text input is needed, such as in forms or comments sections. This component also offers options to customize the appearance and behavior.

## Basic Example

```tsx
import Textarea from './Textarea';

function App() {
  const [value, setValue] = React.useState('');

  const handleChange = (event: React.ChangeEvent<HTMLTextAreaElement>) => {
    setValue(event.target.value);
  };

  return <Textarea id="comment" value={value} onChange={handleChange} placeholder="Enter your comment" />;
}

export default App;
```
In this example, the Textarea component is used with a value and onChange handler to allow the user to type in a comment.

## Textarea Properties

The Textarea component accepts the following props:

- `id` (string): The unique identifier for the textarea element.
- `value` (string): The current value of the textarea.
- `onChange` (function): A callback function to handle changes to the textarea value.
- `placeholder` (string, optional): The placeholder text displayed in the textarea when it's empty.
- `size` ('small' | 'medium' | 'large', optional): Defines the size of the textarea. Defaults to 'medium'.
- `variant` ('default' | 'outlined' | 'filled', optional): Defines the style of the textarea. Defaults to 'default'.
- `disabled` (boolean, optional): Disables the textarea if set to true. Defaults to false.
- `rows` (number, optional): The number of rows to display. Defaults to 4.
- `resize` ('none' | 'both' | 'horizontal' | 'vertical', optional): Controls whether the textarea is resizable. Defaults to 'both'.
- `className` (string, optional): Additional custom CSS classes to apply to the textarea.

## Example with Different Sizes

```tsx
import Textarea from './Textarea';

function App() {
  const [value, setValue] = React.useState('');

  const handleChange = (event: React.ChangeEvent<HTMLTextAreaElement>) => {
    setValue(event.target.value);
  };

  return (
    <div>
      <Textarea id="small" value={value} onChange={handleChange} size="small" placeholder="Small size" />
      <Textarea id="medium" value={value} onChange={handleChange} size="medium" placeholder="Medium size" />
      <Textarea id="large" value={value} onChange={handleChange} size="large" placeholder="Large size" />
    </div>
  );
}

export default App;
```
In this example, three Textarea components are rendered, each with a different size: small, medium, and large.

# Example with Different Variants

```tsx
import Textarea from './Textarea';

function App() {
  const [value, setValue] = React.useState('');

  const handleChange = (event: React.ChangeEvent<HTMLTextAreaElement>) => {
    setValue(event.target.value);
  };

  return (
    <div>
      <Textarea id="default" value={value} onChange={handleChange} variant="default" placeholder="Default variant" />
      <Textarea id="outlined" value={value} onChange={handleChange} variant="outlined" placeholder="Outlined variant" />
      <Textarea id="filled" value={value} onChange={handleChange} variant="filled" placeholder="Filled variant" />
    </div>
  );
}

export default App;
```

This example demonstrates how to use the Textarea component with different variants: default, outlined, and filled.

# Customizing Resizing

```tsx
import Textarea from './Textarea';

function App() {
  const [value, setValue] = React.useState('');

  const handleChange = (event: React.ChangeEvent<HTMLTextAreaElement>) => {
    setValue(event.target.value);
  };

  return (
    <div>
      <Textarea id="resize-none" value={value} onChange={handleChange} resize="none" placeholder="Non-resizable" />
      <Textarea id="resize-both" value={value} onChange={handleChange} resize="both" placeholder="Resizable both" />
      <Textarea id="resize-horizontal" value={value} onChange={handleChange} resize="horizontal" placeholder="Resizable horizontal" />
      <Textarea id="resize-vertical" value={value} onChange={handleChange} resize="vertical" placeholder="Resizable vertical" />
    </div>
  );
}

export default App;
```

This example demonstrates different resize options: none, both, horizontal, and vertical.

# Custom Styling

The Textarea component uses Tailwind CSS classes for styling. You can extend or override the default styles by passing additional className values.

For example:

```tsx
<Textarea id="custom" value={value} onChange={handleChange} className="border-2 border-red-500" />
```

In this example, a custom border color (border-red-500) is applied to the Textarea component.

## Notes

- **Size**: You can adjust the size of the textarea by passing size with one of the following values: 'small', 'medium', or 'large'.
- **Variants**: You can choose from three variants: 'default', 'outlined', and 'filled', each offering a different style for the textarea.
- **Resizing**: The resize property determines if the textarea can be resized. By default, it's resizable in both directions (both), but you can restrict this to horizontal, vertical, or disable resizing entirely by setting it to none.